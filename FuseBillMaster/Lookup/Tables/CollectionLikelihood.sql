CREATE TABLE [Lookup].[CollectionLikelihood] (
    [Id]   TINYINT      NOT NULL,
    [Name] VARCHAR (20) NOT NULL,
    CONSTRAINT [PK_CollectionLikelihood] PRIMARY KEY CLUSTERED ([Id] ASC) WITH (FILLFACTOR = 100)
);


GO

